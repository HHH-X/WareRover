{"id": "80a20b4f-9eb1-4aa6-8da0-0058bb2216e5", "code": "# EVOLVE-BLOCK-START\n\n# -------- Planner Candidate --------\n\nimport heapq\nfrom typing import Dict, Tuple, List, Set\nfrom collections import defaultdict\nfrom planner.base_planner import BasePlanner\nfrom utils.simulation_context import SimulationContext\n\nMAX_ASTAR_NODES = 800\n\nclass AStarPlanner(BasePlanner):\n    def __init__(self, ctx: SimulationContext):\n        super().__init__(ctx)\n        self.env = ctx.env\n        self.agv_manager = ctx.agv_manager\n        self.order_manager = ctx.order_manager\n        self.map = ctx.grid_map\n        self.fault_manager = ctx.fault_manager\n        self.max_time = 100\n        env_info = self.env.get_env_info()\n        self.agv_sizes = env_info['agv_sizes']\n\n    def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[int, List[Tuple[int, int]]]:\n        \"\"\"\n        Centralized path planning using A* with a reservation table (avoids vertex and edge conflicts).\n        Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes start).\n        \"\"\"\n        if not targets:\n            return {}\n        env_info = self.env.get_env_info()\n        current_paths = env_info['action_queues']\n        carrying_status = env_info['carrying_status']\n        reservation_table = self._build_reservation_table(current_paths)\n\n        # Sort AGVs by distance to their goals to potentially improve planning order\n        agv_list = list(targets.items())\n        agv_list.sort(key=lambda item: self._heuristic(item[1][0], item[1][1]))\n        \n        paths = {}\n        for agv_id, (start, goal) in agv_list:\n            carrying = carrying_status.get(agv_id, False)\n            path = self._a_star_with_reservation(\n                agv_id, start, goal, carrying, reservation_table\n            )\n            # If A* fails, try a simpler approach: wait in place for a few steps then retry\n            if path is None:\n                # Create a waiting path (stay in place for 5 steps)\n                wait_path = [start] * 5\n                paths[agv_id] = wait_path[1:] if len(wait_path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, wait_path)\n            else:\n                paths[agv_id] = path[1:] if len(path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, path)\n        return paths\n\n    def _build_reservation_table(\n        self, current_paths: Dict[int, List[Tuple[int, int]]],\n    ) -> Dict[int, Set[Tuple[int, int]]]:\n        \"\"\"Build reservation table: time step -> set of occupied cells (AGV size-aware).\"\"\"\n        table = defaultdict(set)\n        for agv_id, path in current_paths.items():\n            for t, pos in enumerate(path):\n                occupied = self._get_occupied_cells(agv_id, pos)\n                for cell in occupied:\n                    table[t].add(cell)\n        return table\n\n    def _add_to_reservation_table(\n        self, agv_id: int,\n        table: Dict[int, Set[Tuple[int, int]]],\n        path: List[Tuple[int, int]]\n    ):\n        \"\"\"Add a new path to the reservation table (AGV size-aware).\"\"\"\n        for t, pos in enumerate(path):\n            occupied = self._get_occupied_cells(agv_id, pos)\n            for cell in occupied:\n                table[t].add(cell)\n\n    def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                 carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                 ) -> List[Tuple[int, int]]:\n        \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n        open_set = []\n        heapq.heappush(open_set, (0 + self._heuristic(start, goal), 0, start, [start]))\n        closed_set = set()\n        expanded_nodes = 0\n        while open_set:\n            expanded_nodes += 1\n            if expanded_nodes >= MAX_ASTAR_NODES:\n                break\n            f, g, current, path = heapq.heappop(open_set)\n            if (current, g) in closed_set:\n                continue\n            closed_set.add((current, g))\n\n            if current == goal:\n                # Check if we can stay at goal for 2 time steps to ensure stability\n                if self._is_free(agv_id, current, g + 1, reservation_table) and \\\n                   self._is_free(agv_id, current, g + 2, reservation_table):\n                    return path + [goal] * 2\n                # If not, we can still consider it as a valid path but mark it\n                # For now, we'll accept it with just one extra step\n                elif self._is_free(agv_id, current, g + 1, reservation_table):\n                    return path + [goal]\n\n            for neighbor in self.env.get_walkable_neighbors(agv_id, current, carrying):\n                if not self._is_free(agv_id, neighbor, g + 1, reservation_table):\n                    continue\n                if self._is_edge_conflict(agv_id, current, neighbor, g + 1, reservation_table):\n                    continue\n\n                new_path = path + [neighbor]\n                heapq.heappush(open_set, (g + 1 + self._heuristic(neighbor, goal), g + 1, neighbor, new_path))\n\n            if g > self.max_time:\n                break\n        return None\n\n    def _heuristic(self, a: Tuple[int, int], b: Tuple[int, int]) -> int:\n        \"\"\"Manhattan distance heuristic.\"\"\"\n        return abs(a[0] - b[0]) + abs(a[1] - b[1])\n\n    def _is_free(self, agv_id: int, pos: Tuple[int, int],\n                 t: int, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                 ) -> bool:\n        \"\"\"True if at time t the AGV at top-left pos and its occupied cells are all free in the table.\"\"\"\n        for cell in self._get_occupied_cells(agv_id, pos):\n            if cell in reservation_table.get(t, set()):\n                return False\n        return True\n\n    def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                      reservation_table: Dict[int, Set[Tuple[int, int]]]\n                      ) -> bool:\n        \"\"\"Edge/swap conflict: check if any AGV occupies to_pos at time t-1 and from_pos at time t.\"\"\"\n        # Standard swap conflict: two AGVs want to swap positions in one time step\n        # For the moving AGV, at time t-1 it's at from_pos, at time t it wants to be at to_pos\n        # Check if any other AGV is at to_pos at time t-1 and wants to move to from_pos at time t\n        # Since we're checking for the current AGV, we need to see if to_pos is occupied at time t-1\n        occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n        occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n        \n        # Check if to_pos is occupied at time t-1 by any AGV (including the current one?)\n        # Since reservation_table doesn't track AGV IDs, we need to be careful\n        # Actually, the current AGV won't be in the reservation table at time t-1 yet\n        # So just check if any cell in occ_to_tminus1 is in reservation_table[t-1]\n        if any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1):\n            # Also check if from_pos would be occupied at time t by the same AGV?\n            # For simplicity, we'll be conservative and prevent the move\n            return True\n        return False\n\n    def _get_occupied_cells(self, agv_id: int, top_left: Tuple[int, int]) -> set[Tuple[int, int]]:\n        \"\"\"Return set of grid cells occupied by this AGV given its top-left and size.\"\"\"\n        size = self.agv_sizes.get(agv_id, 1)\n        x, y = top_left\n        return {(x + dx, y + dy) for dx in range(size) for dy in range(size)}\n\n# EVOLVE-BLOCK-END\n", "changes_description": null, "language": "python", "parent_id": "05cfd560-61c5-49d7-934e-708addba3eb7", "generation": 1, "timestamp": 1775467999.1432123, "iteration_found": 1, "metrics": {"completion_score": 1.0, "makespan_score": 0.0013066202090592333, "time_score": 0.2845532482361767, "stability_score": 1.0, "combined_score": 0.5073679667204061, "completion_mean": 1.0, "makespan_mean": 764.3333333333334, "planner_time_mean": 0.002514280740770208, "stability_ratio": 1.0, "seed_count": 3.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                        reservation_table: Dict[int, Set[Tuple[int, int]]]\n                        ) -> bool:\n          \"\"\"Edge/swap conflict: from_pos occupied at t and to_pos occupied at t-1 overlap with tab...\n          occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n          occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n          conflict_now = any(cell in reservation_table.get(t, set()) for cell in occ_from_t)\n          conflict_prev = any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1)\n          return conflict_now and conflict_prev\nwith:\n  def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                        reservation_table: Dict[int, Set[Tuple[int, int]]]\n                        ) -> bool:\n          \"\"\"Edge/swap conflict: check if any AGV occupies to_pos at time t-1 and from_pos at time ...\n          # Standard swap conflict: two AGVs want to swap positions in one time step\n          # For the moving AGV, at time t-1 it's at from_pos, at time t it wants to be at to_pos\n          # Check if any other AGV is at to_pos at time t-1 and wants to move to from_pos at time t\n          # Since we're checking for the current AGV, we need to see if to_pos is occupied at time t-1\n          occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n          occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n  \n          # Check if to_pos is occupied at time t-1 by any AGV (including the current one?)\n          # Since reservation_table doesn't track AGV IDs, we need to be careful\n          # Actually, the current AGV won't be in the reservation table at time t-1 yet\n          # So just check if any cell in occ_to_tminus1 is in reservation_table[t-1]\n          if any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1):\n              # Also check if from_pos would be occupied at time t by the same AGV?\n              # For simplicity, we'll be conservative and prevent the move\n              return True\n          return False\nChange 2: Replace:\n  def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[in...\n          \"\"\"\n          Centralized path planning using A* with a reservation table (avoids vertex and edge confl...\n          Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes s...\n          \"\"\"\n          if not targets:\n              return {}\n          env_info = self.env.get_env_info()\n          current_paths = env_info['action_queues']\n          carrying_status = env_info['carrying_status']\n          reservation_table = self._build_reservation_table(current_paths)\n  \n          paths = {}\n          for agv_id, (start, goal) in targets.items():\n              carrying = carrying_status.get(agv_id, False)\n              path = self._a_star_with_reservation(\n                  agv_id, start, goal, carrying, reservation_table\n              )\n              if path:\n                  paths[agv_id] = path[1:] if len(path) > 1 else []\n                  self._add_to_reservation_table(agv_id, reservation_table, path)\n              else:\n                  paths[agv_id] = [start]\n          return paths\nwith:\n  def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[in...\n          \"\"\"\n          Centralized path planning using A* with a reservation table (avoids vertex and edge confl...\n          Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes s...\n          \"\"\"\n          if not targets:\n              return {}\n          env_info = self.env.get_env_info()\n          current_paths = env_info['action_queues']\n          carrying_status = env_info['carrying_status']\n          reservation_table = self._build_reservation_table(current_paths)\n  \n          # Sort AGVs by distance to their goals to potentially improve planning order\n          agv_list = list(targets.items())\n          agv_list.sort(key=lambda item: self._heuristic(item[1][0], item[1][1]))\n  \n          paths = {}\n          for agv_id, (start, goal) in agv_list:\n              carrying = carrying_status.get(agv_id, False)\n              path = self._a_star_with_reservation(\n                  agv_id, start, goal, carrying, reservation_table\n              )\n              # If A* fails, try a simpler approach: wait in place for a few steps then retry\n              if path is None:\n                  # Create a waiting path (stay in place for 5 steps)\n                  wait_path = [start] * 5\n                  paths[agv_id] = wait_path[1:] if len(wait_path) > 1 else []\n                  self._add_to_reservation_table(agv_id, reservation_table, wait_path)\n              else:\n                  paths[agv_id] = path[1:] if len(path) > 1 else []\n  ... (2 more lines)\nChange 3: Replace:\n  if current == goal and self._is_free(agv_id, current, g + 1, reservation_table) and self._is_free...\n                  return path + [goal] * 2\nwith:\n  if current == goal:\n                  # Check if we can stay at goal for 2 time steps to ensure stability\n                  if self._is_free(agv_id, current, g + 1, reservation_table) and \\\n                     self._is_free(agv_id, current, g + 2, reservation_table):\n                      return path + [goal] * 2\n                  # If not, we can still consider it as a valid path but mark it\n                  # For now, we'll accept it with just one extra step\n                  elif self._is_free(agv_id, current, g + 1, reservation_table):\n                      return path + [goal]\nChange 4: Replace:\n  def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                   carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                   ) -> List[Tuple[int, int]]:\n          \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n          open_set = []\n          heapq.heappush(open_set, (0 + self._heuristic(start, goal), 0, start, [start]))\n          closed_set = set()\n          expanded_nodes = 0\n          while open_set:\n              expanded_nodes += 1\n              if expanded_nodes >= MAX_ASTAR_NODES:\n                  break\n              f, g, current, path = heapq.heappop(open_set)\n              if (current, g) in closed_set:\n                  continue\n              closed_set.add((current, g))\n  \n              if current == goal and self._is_free(agv_id, current, g + 1, reservation_table) and s...\n                  return path + [goal] * 2\n  \n              for neighbor in self.env.get_walkable_neighbors(agv_id, current, carrying):\n                  if not self._is_free(agv_id, neighbor, g + 1, reservation_table):\n                      continue\n                  if self._is_edge_conflict(agv_id, current, neighbor, g + 1, reservation_table):\n                      continue\n  \n                  new_path = path + [neighbor]\n                  heapq.heappush(open_set, (g + 1 + self._heuristic(neighbor, goal), g + 1, neighbo...\n  \n              if g > self.max_time:\n  ... (2 more lines)\nwith:\n  def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                   carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                   ) -> List[Tuple[int, int]]:\n          \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n          open_set = []\n          heapq.heappush(open_set, (self._heuristic(start, goal), 0, start, [start]))\n          closed_set = {}\n          expanded_nodes = 0\n  \n          # Cache for get_walkable_neighbors to avoid repeated calls\n          neighbor_cache = {}\n  \n          while open_set:\n              expanded_nodes += 1\n              if expanded_nodes >= MAX_ASTAR_NODES:\n                  break\n              f, g, current, path = heapq.heappop(open_set)\n  \n              # Check closed set with g-value\n              if closed_set.get(current, float('inf')) <= g:\n                  continue\n              closed_set[current] = g\n  \n              # Goal check with more flexible conditions\n              if current == goal:\n                  # Try to stay at goal for 2 steps, but accept 1 if necessary\n                  if self._is_free(agv_id, current, g + 1, reservation_table):\n                      if self._is_free(agv_id, current, g + 2, reservation_table):\n                          return path + [goal] * 2\n                      else:\n  ... (23 more lines)", "parent_metrics": {"completion_score": 1.0, "makespan_score": 0.0014563106796116505, "time_score": 0.1441055121914953, "stability_score": 1.0, "combined_score": 0.47933081117616316, "completion_mean": 1.0, "makespan_mean": 685.6666666666666, "planner_time_mean": 0.005939359812073983, "stability_ratio": 1.0, "seed_count": 3.0}, "island": 0}, "prompts": {"diff_user": {"system": "Optimize complete MAPF planner implementation class code while preserving interface compatibility and runtime robustness.", "user": "# Current Program Information\n- Fitness: 0.4793\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at 0.4793\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: completion_score: 1.0000, makespan_score: 0.0015, time_score: 0.1441, stability_score: 1.0000, combined_score: 0.4793, completion_mean: 1.0000, makespan_mean: 685.6667, planner_time_mean: 0.0059, stability_ratio: 1.0000, seed_count: 3.0000\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: 0.4793)\n```python\n# EVOLVE-BLOCK-START\n\n# -------- Planner Candidate --------\n\nimport heapq\nfrom typing import Dict, Tuple, List, Set\nfrom collections import defaultdict\nfrom planner.base_planner import BasePlanner\nfrom utils.simulation_context import SimulationContext\n\nMAX_ASTAR_NODES = 800\n\nclass AStarPlanner(BasePlanner):\n    def __init__(self, ctx: SimulationContext):\n        super().__init__(ctx)\n        self.env = ctx.env\n        self.agv_manager = ctx.agv_manager\n        self.order_manager = ctx.order_manager\n        self.map = ctx.grid_map\n        self.fault_manager = ctx.fault_manager\n        self.max_time = 100\n        env_info = self.env.get_env_info()\n        self.agv_sizes = env_info['agv_sizes']\n\n    def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[int, List[Tuple[int, int]]]:\n        \"\"\"\n        Centralized path planning using A* with a reservation table (avoids vertex and edge conflicts).\n        Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes start).\n        \"\"\"\n        if not targets:\n            return {}\n        env_info = self.env.get_env_info()\n        current_paths = env_info['action_queues']\n        carrying_status = env_info['carrying_status']\n        reservation_table = self._build_reservation_table(current_paths)\n\n        paths = {}\n        for agv_id, (start, goal) in targets.items():\n            carrying = carrying_status.get(agv_id, False)\n            path = self._a_star_with_reservation(\n                agv_id, start, goal, carrying, reservation_table\n            )\n            if path:\n                paths[agv_id] = path[1:] if len(path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, path)\n            else:\n                paths[agv_id] = [start]\n        return paths\n\n    def _build_reservation_table(\n        self, current_paths: Dict[int, List[Tuple[int, int]]],\n    ) -> Dict[int, Set[Tuple[int, int]]]:\n        \"\"\"Build reservation table: time step -> set of occupied cells (AGV size-aware).\"\"\"\n        table = defaultdict(set)\n        for agv_id, path in current_paths.items():\n            for t, pos in enumerate(path):\n                occupied = self._get_occupied_cells(agv_id, pos)\n                for cell in occupied:\n                    table[t].add(cell)\n        return table\n\n    def _add_to_reservation_table(\n        self, agv_id: int,\n        table: Dict[int, Set[Tuple[int, int]]],\n        path: List[Tuple[int, int]]\n    ):\n        \"\"\"Add a new path to the reservation table (AGV size-aware).\"\"\"\n        for t, pos in enumerate(path):\n            occupied = self._get_occupied_cells(agv_id, pos)\n            for cell in occupied:\n                table[t].add(cell)\n\n    def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                 carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                 ) -> List[Tuple[int, int]]:\n        \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n        open_set = []\n        heapq.heappush(open_set, (0 + self._heuristic(start, goal), 0, start, [start]))\n        closed_set = set()\n        expanded_nodes = 0\n        while open_set:\n            expanded_nodes += 1\n            if expanded_nodes >= MAX_ASTAR_NODES:\n                break\n            f, g, current, path = heapq.heappop(open_set)\n            if (current, g) in closed_set:\n                continue\n            closed_set.add((current, g))\n\n            if current == goal and self._is_free(agv_id, current, g + 1, reservation_table) and self._is_free(agv_id, current, g + 2, reservation_table):\n                return path + [goal] * 2\n\n            for neighbor in self.env.get_walkable_neighbors(agv_id, current, carrying):\n                if not self._is_free(agv_id, neighbor, g + 1, reservation_table):\n                    continue\n                if self._is_edge_conflict(agv_id, current, neighbor, g + 1, reservation_table):\n                    continue\n\n                new_path = path + [neighbor]\n                heapq.heappush(open_set, (g + 1 + self._heuristic(neighbor, goal), g + 1, neighbor, new_path))\n\n            if g > self.max_time:\n                break\n        return None\n\n    def _heuristic(self, a: Tuple[int, int], b: Tuple[int, int]) -> int:\n        \"\"\"Manhattan distance heuristic.\"\"\"\n        return abs(a[0] - b[0]) + abs(a[1] - b[1])\n\n    def _is_free(self, agv_id: int, pos: Tuple[int, int],\n                 t: int, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                 ) -> bool:\n        \"\"\"True if at time t the AGV at top-left pos and its occupied cells are all free in the table.\"\"\"\n        for cell in self._get_occupied_cells(agv_id, pos):\n            if cell in reservation_table.get(t, set()):\n                return False\n        return True\n\n    def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                      reservation_table: Dict[int, Set[Tuple[int, int]]]\n                      ) -> bool:\n        \"\"\"Edge/swap conflict: from_pos occupied at t and to_pos occupied at t-1 overlap with table.\"\"\"\n        occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n        occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n        conflict_now = any(cell in reservation_table.get(t, set()) for cell in occ_from_t)\n        conflict_prev = any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1)\n        return conflict_now and conflict_prev\n\n    def _get_occupied_cells(self, agv_id: int, top_left: Tuple[int, int]) -> set[Tuple[int, int]]:\n        \"\"\"Return set of grid cells occupied by this AGV given its top-left and size.\"\"\"\n        size = self.agv_sizes.get(agv_id, 1)\n        x, y = top_left\n        return {(x + dx, y + dy) for dx in range(size) for dy in range(size)}\n\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on completion_score (1.0000), Performs well on makespan_score (0.0015), Performs well on time_score (0.1441), Performs well on stability_score (1.0000), Performs well on combined_score (0.4793), Performs well on completion_mean (1.0000), Performs well on makespan_mean (685.6667), Performs well on planner_time_mean (0.0059), Performs well on stability_ratio (1.0000), Performs well on seed_count (3.0000)\n\n\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n\n# -------- Planner Candidate --------\n\nimport heapq\nfrom typing import Dict, Tuple, List, Set\nfrom collections import defaultdict\nfrom planner.base_planner import BasePlanner\nfrom utils.simulation_context import SimulationContext\n\nMAX_ASTAR_NODES = 800\n\nclass AStarPlanner(BasePlanner):\n    def __init__(self, ctx: SimulationContext):\n        super().__init__(ctx)\n        self.env = ctx.env\n        self.agv_manager = ctx.agv_manager\n        self.order_manager = ctx.order_manager\n        self.map = ctx.grid_map\n        self.fault_manager = ctx.fault_manager\n        self.max_time = 100\n        env_info = self.env.get_env_info()\n        self.agv_sizes = env_info['agv_sizes']\n\n    def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[int, List[Tuple[int, int]]]:\n        \"\"\"\n        Centralized path planning using A* with a reservation table (avoids vertex and edge conflicts).\n        Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes start).\n        \"\"\"\n        if not targets:\n            return {}\n        env_info = self.env.get_env_info()\n        current_paths = env_info['action_queues']\n        carrying_status = env_info['carrying_status']\n        reservation_table = self._build_reservation_table(current_paths)\n\n        paths = {}\n        for agv_id, (start, goal) in targets.items():\n            carrying = carrying_status.get(agv_id, False)\n            path = self._a_star_with_reservation(\n                agv_id, start, goal, carrying, reservation_table\n            )\n            if path:\n                paths[agv_id] = path[1:] if len(path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, path)\n            else:\n                paths[agv_id] = [start]\n        return paths\n\n    def _build_reservation_table(\n        self, current_paths: Dict[int, List[Tuple[int, int]]],\n    ) -> Dict[int, Set[Tuple[int, int]]]:\n        \"\"\"Build reservation table: time step -> set of occupied cells (AGV size-aware).\"\"\"\n        table = defaultdict(set)\n        for agv_id, path in current_paths.items():\n            for t, pos in enumerate(path):\n                occupied = self._get_occupied_cells(agv_id, pos)\n                for cell in occupied:\n                    table[t].add(cell)\n        return table\n\n    def _add_to_reservation_table(\n        self, agv_id: int,\n        table: Dict[int, Set[Tuple[int, int]]],\n        path: List[Tuple[int, int]]\n    ):\n        \"\"\"Add a new path to the reservation table (AGV size-aware).\"\"\"\n        for t, pos in enumerate(path):\n            occupied = self._get_occupied_cells(agv_id, pos)\n            for cell in occupied:\n                table[t].add(cell)\n\n    def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                 carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                 ) -> List[Tuple[int, int]]:\n        \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n        open_set = []\n        heapq.heappush(open_set, (0 + self._heuristic(start, goal), 0, start, [start]))\n        closed_set = set()\n        expanded_nodes = 0\n        while open_set:\n            expanded_nodes += 1\n            if expanded_nodes >= MAX_ASTAR_NODES:\n                break\n            f, g, current, path = heapq.heappop(open_set)\n            if (current, g) in closed_set:\n                continue\n            closed_set.add((current, g))\n\n            if current == goal and self._is_free(agv_id, current, g + 1, reservation_table) and self._is_free(agv_id, current, g + 2, reservation_table):\n                return path + [goal] * 2\n\n            for neighbor in self.env.get_walkable_neighbors(agv_id, current, carrying):\n                if not self._is_free(agv_id, neighbor, g + 1, reservation_table):\n                    continue\n                if self._is_edge_conflict(agv_id, current, neighbor, g + 1, reservation_table):\n                    continue\n\n                new_path = path + [neighbor]\n                heapq.heappush(open_set, (g + 1 + self._heuristic(neighbor, goal), g + 1, neighbor, new_path))\n\n            if g > self.max_time:\n                break\n        return None\n\n    def _heuristic(self, a: Tuple[int, int], b: Tuple[int, int]) -> int:\n        \"\"\"Manhattan distance heuristic.\"\"\"\n        return abs(a[0] - b[0]) + abs(a[1] - b[1])\n\n    def _is_free(self, agv_id: int, pos: Tuple[int, int],\n                 t: int, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                 ) -> bool:\n        \"\"\"True if at time t the AGV at top-left pos and its occupied cells are all free in the table.\"\"\"\n        for cell in self._get_occupied_cells(agv_id, pos):\n            if cell in reservation_table.get(t, set()):\n                return False\n        return True\n\n    def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                      reservation_table: Dict[int, Set[Tuple[int, int]]]\n                      ) -> bool:\n        \"\"\"Edge/swap conflict: from_pos occupied at t and to_pos occupied at t-1 overlap with table.\"\"\"\n        occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n        occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n        conflict_now = any(cell in reservation_table.get(t, set()) for cell in occ_from_t)\n        conflict_prev = any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1)\n        return conflict_now and conflict_prev\n\n    def _get_occupied_cells(self, agv_id: int, top_left: Tuple[int, int]) -> set[Tuple[int, int]]:\n        \"\"\"Return set of grid cells occupied by this AGV given its top-left and size.\"\"\"\n        size = self.agv_sizes.get(agv_id, 1)\n        x, y = top_left\n        return {(x + dx, y + dy) for dx in range(size) for dy in range(size)}\n\n# EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program, I see a few areas where we can optimize and improve robustness while maintaining the same interface and functionality. The current implementation has some inefficiencies and potential issues:\n\n1. The reservation table building uses `defaultdict(set)` which is fine, but the edge conflict check may be overly restrictive.\n2. The A* implementation always adds two extra goal positions at the end, which may not always be necessary.\n3. The `_is_edge_conflict` function has a logical issue: it's checking if BOTH current position at time t AND previous position at time t-1 have conflicts, which is not the standard swap conflict definition.\n4. The planner could be more efficient by reordering AGV planning based on distance or other heuristics.\n5. There's no fallback when A* fails to find a path within node limit or time limit.\n\nLet's focus on improving the edge conflict detection and adding a simple fallback mechanism:\n\n<<<<<<< SEARCH\n    def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                      reservation_table: Dict[int, Set[Tuple[int, int]]]\n                      ) -> bool:\n        \"\"\"Edge/swap conflict: from_pos occupied at t and to_pos occupied at t-1 overlap with table.\"\"\"\n        occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n        occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n        conflict_now = any(cell in reservation_table.get(t, set()) for cell in occ_from_t)\n        conflict_prev = any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1)\n        return conflict_now and conflict_prev\n=======\n    def _is_edge_conflict(self, agv_id: int, from_pos: Tuple[int, int], to_pos: Tuple[int, int], t: int,\n                      reservation_table: Dict[int, Set[Tuple[int, int]]]\n                      ) -> bool:\n        \"\"\"Edge/swap conflict: check if any AGV occupies to_pos at time t-1 and from_pos at time t.\"\"\"\n        # Standard swap conflict: two AGVs want to swap positions in one time step\n        # For the moving AGV, at time t-1 it's at from_pos, at time t it wants to be at to_pos\n        # Check if any other AGV is at to_pos at time t-1 and wants to move to from_pos at time t\n        # Since we're checking for the current AGV, we need to see if to_pos is occupied at time t-1\n        occ_to_tminus1 = self._get_occupied_cells(agv_id, to_pos)\n        occ_from_t = self._get_occupied_cells(agv_id, from_pos)\n        \n        # Check if to_pos is occupied at time t-1 by any AGV (including the current one?)\n        # Since reservation_table doesn't track AGV IDs, we need to be careful\n        # Actually, the current AGV won't be in the reservation table at time t-1 yet\n        # So just check if any cell in occ_to_tminus1 is in reservation_table[t-1]\n        if any(cell in reservation_table.get(t - 1, set()) for cell in occ_to_tminus1):\n            # Also check if from_pos would be occupied at time t by the same AGV?\n            # For simplicity, we'll be conservative and prevent the move\n            return True\n        return False\n>>>>>>> REPLACE\n\nNow, let's improve the A* search to have a fallback when it can't find a path:\n\n<<<<<<< SEARCH\n    def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[int, List[Tuple[int, int]]]:\n        \"\"\"\n        Centralized path planning using A* with a reservation table (avoids vertex and edge conflicts).\n        Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes start).\n        \"\"\"\n        if not targets:\n            return {}\n        env_info = self.env.get_env_info()\n        current_paths = env_info['action_queues']\n        carrying_status = env_info['carrying_status']\n        reservation_table = self._build_reservation_table(current_paths)\n\n        paths = {}\n        for agv_id, (start, goal) in targets.items():\n            carrying = carrying_status.get(agv_id, False)\n            path = self._a_star_with_reservation(\n                agv_id, start, goal, carrying, reservation_table\n            )\n            if path:\n                paths[agv_id] = path[1:] if len(path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, path)\n            else:\n                paths[agv_id] = [start]\n        return paths\n=======\n    def plan(self, targets: Dict[int, Tuple[Tuple[int, int], Tuple[int, int]]], scheduler) -> Dict[int, List[Tuple[int, int]]]:\n        \"\"\"\n        Centralized path planning using A* with a reservation table (avoids vertex and edge conflicts).\n        Args: targets {agv_id: (start_pos, target_pos)}. Returns: {agv_id: path} (path excludes start).\n        \"\"\"\n        if not targets:\n            return {}\n        env_info = self.env.get_env_info()\n        current_paths = env_info['action_queues']\n        carrying_status = env_info['carrying_status']\n        reservation_table = self._build_reservation_table(current_paths)\n\n        # Sort AGVs by distance to their goals to potentially improve planning order\n        agv_list = list(targets.items())\n        agv_list.sort(key=lambda item: self._heuristic(item[1][0], item[1][1]))\n        \n        paths = {}\n        for agv_id, (start, goal) in agv_list:\n            carrying = carrying_status.get(agv_id, False)\n            path = self._a_star_with_reservation(\n                agv_id, start, goal, carrying, reservation_table\n            )\n            # If A* fails, try a simpler approach: wait in place for a few steps then retry\n            if path is None:\n                # Create a waiting path (stay in place for 5 steps)\n                wait_path = [start] * 5\n                paths[agv_id] = wait_path[1:] if len(wait_path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, wait_path)\n            else:\n                paths[agv_id] = path[1:] if len(path) > 1 else []\n                self._add_to_reservation_table(agv_id, reservation_table, path)\n        return paths\n>>>>>>> REPLACE\n\nLet's also optimize the goal check in A* to be more flexible:\n\n<<<<<<< SEARCH\n            if current == goal and self._is_free(agv_id, current, g + 1, reservation_table) and self._is_free(agv_id, current, g + 2, reservation_table):\n                return path + [goal] * 2\n=======\n            if current == goal:\n                # Check if we can stay at goal for 2 time steps to ensure stability\n                if self._is_free(agv_id, current, g + 1, reservation_table) and \\\n                   self._is_free(agv_id, current, g + 2, reservation_table):\n                    return path + [goal] * 2\n                # If not, we can still consider it as a valid path but mark it\n                # For now, we'll accept it with just one extra step\n                elif self._is_free(agv_id, current, g + 1, reservation_table):\n                    return path + [goal]\n>>>>>>> REPLACE\n\nFinally, let's add a small optimization to reduce the number of nodes expanded:\n\n<<<<<<< SEARCH\n    def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                 carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                 ) -> List[Tuple[int, int]]:\n        \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n        open_set = []\n        heapq.heappush(open_set, (0 + self._heuristic(start, goal), 0, start, [start]))\n        closed_set = set()\n        expanded_nodes = 0\n        while open_set:\n            expanded_nodes += 1\n            if expanded_nodes >= MAX_ASTAR_NODES:\n                break\n            f, g, current, path = heapq.heappop(open_set)\n            if (current, g) in closed_set:\n                continue\n            closed_set.add((current, g))\n\n            if current == goal and self._is_free(agv_id, current, g + 1, reservation_table) and self._is_free(agv_id, current, g + 2, reservation_table):\n                return path + [goal] * 2\n\n            for neighbor in self.env.get_walkable_neighbors(agv_id, current, carrying):\n                if not self._is_free(agv_id, neighbor, g + 1, reservation_table):\n                    continue\n                if self._is_edge_conflict(agv_id, current, neighbor, g + 1, reservation_table):\n                    continue\n\n                new_path = path + [neighbor]\n                heapq.heappush(open_set, (g + 1 + self._heuristic(neighbor, goal), g + 1, neighbor, new_path))\n\n            if g > self.max_time:\n                break\n        return None\n=======\n    def _a_star_with_reservation(self, agv_id: int, start: Tuple[int, int], goal: Tuple[int, int],\n                                 carrying: bool, reservation_table: Dict[int, Set[Tuple[int, int]]]\n                                 ) -> List[Tuple[int, int]]:\n        \"\"\"A* with reservation table to avoid vertex and swap conflicts.\"\"\"\n        open_set = []\n        heapq.heappush(open_set, (self._heuristic(start, goal), 0, start, [start]))\n        closed_set = {}\n        expanded_nodes = 0\n        \n        # Cache for get_walkable_neighbors to avoid repeated calls\n        neighbor_cache = {}\n        \n        while open_set:\n            expanded_nodes += 1\n            if expanded_nodes >= MAX_ASTAR_NODES:\n                break\n            f, g, current, path = heapq.heappop(open_set)\n            \n            # Check closed set with g-value\n            if closed_set.get(current, float('inf')) <= g:\n                continue\n            closed_set[current] = g\n\n            # Goal check with more flexible conditions\n            if current == goal:\n                # Try to stay at goal for 2 steps, but accept 1 if necessary\n                if self._is_free(agv_id, current, g + 1, reservation_table):\n                    if self._is_free(agv_id, current, g + 2, reservation_table):\n                        return path + [goal] * 2\n                    else:\n                        return path + [goal]\n\n            # Get neighbors from cache or compute\n            if (current, carrying) not in neighbor_cache:\n                neighbor_cache[(current, carrying)] = list(self.env.get_walkable_neighbors(agv_id, current, carrying))\n            neighbors = neighbor_cache[(current, carrying)]\n            \n            for neighbor in neighbors:\n                # Check vertex conflict at time g+1\n                if not self._is_free(agv_id, neighbor, g + 1, reservation_table):\n                    continue\n                # Check edge conflict\n                if self._is_edge_conflict(agv_id, current, neighbor, g + 1, reservation_table):\n                    continue\n                \n                new_g = g + 1\n                new_f = new_g + self._heuristic(neighbor, goal)\n                new_path = path + [neighbor]\n                heapq.heappush(open_set, (new_f, new_g, neighbor, new_path))\n\n            if g > self.max_time:\n                break\n        return None\n>>>>>>> REPLACE\n\nThese changes should improve the planner's robustness and performance:\n1. Fixed the edge conflict detection to be more accurate\n2. Added sorting of AGVs by distance to goal for better planning order\n3. Added fallback waiting behavior when A* fails\n4. Made goal condition more flexible\n5. Added caching for walkable neighbors to improve performance\n6. Used a more efficient closed set structure\n\nThese improvements should help maintain or improve the fitness score by making the planner more robust while keeping the same interface."]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}